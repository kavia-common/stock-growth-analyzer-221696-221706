# Frontend environment example - Stock Growth Analyzer (React)

# Base URL of the FastAPI backend. If unset, the app tries to infer by replacing :3000 -> :3001 on current origin.
# Local development:
# REACT_APP_BACKEND_URL=http://localhost:3001
#
# Cloud preview (must be HTTPS and same host as the frontend, port 3001):
# Example (replace host accordingly):
# REACT_APP_BACKEND_URL=https://vscode-internal-13394-beta.beta01.cloud.kavia.ai:3001
REACT_APP_BACKEND_URL=
